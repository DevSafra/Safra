node_modules
dist
.next
.turbo
pnpm-lock.yaml
packages/db/migrations

# Third-party agent tooling installed into this working tree (2026-09-02).
#
# `.gitignore` already excludes all of it — `df67d31` did that for the skills, and the
# impeccable hook writes its own cache under `.impeccable/`. Prettier does not read
# `.gitignore`, so without these lines `pnpm verify` fails on a JSON file no human wrote
# and no build consumes, in every checkout where a skill has been run.
.claude
.agents
.impeccable
skills-lock.json

# The RTL text plugin, copied out of node_modules at build time (2026-09-17).
#
# `apps/web/.gitignore` excludes it and no human edits it — it is somebody else's compiled
# output plus their licence file. Prettier does not read `.gitignore`, so without this the
# whole of `pnpm verify` fails on a vendored LICENSE.md the moment anybody runs a build.
apps/web/public/map
apps/partner/public/map
